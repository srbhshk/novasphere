# @novasphere/agent-core

The AI adapter system. Framework-agnostic TypeScript — no React, no CSS, no UI. Contains the AgentAdapter interface, adapter factory, and concrete adapter implementations.

Coming soon.
