# @novasphere/tenant-core

The multi-tenancy system. Framework-agnostic TypeScript — no React, no CSS. Contains TenantConfig types, the static tenant registry, and tenant resolution utilities.

Coming soon.
